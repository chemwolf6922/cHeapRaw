# cheap

A simple C heap (priority queue) library.

## Build

```bash
cmake -B build
cmake --build build
```

## Run tests

```bash
./build/test_function
./build/test_perf
```

## Usage

See [heap.h](heap.h) for the API.
